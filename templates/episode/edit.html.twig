{% extends 'base.html.twig' %}
{% block title %}Editer Episode
{% endblock %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Editer Episode S{{ "%02d"|format(episode.season.number) }}-E{{ "%02d"|format(episode.number) }}</h1>

		{{ include('episode/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('app_episode_index') }}" class="btn btn-primary btn-sm">Retour à la liste</a>

		{{ include('episode/_delete_form.html.twig') }}
	</div>
{% endblock %}
